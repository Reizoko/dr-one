int main(int argc, char* argv[]) {
  /**
  * Program entry point
  *   @param argc : Command-line argument count
  *   @param argv : Array of command line arguments
  *   @returns      A number corresponding to the exit state (0 : no error)
  */

  return 0;
}
